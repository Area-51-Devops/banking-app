name: Transaction-Service-CI

on:
  push:
    paths:
      - 'services/transaction-service/**'
  pull_request:
    paths:
      - 'services/transaction-service/**'
  workflow_dispatch:

jobs:
  call-common:
    uses: ./.github/workflows/common-ci.yml
    with:
      working_directory: services/transaction-service
      image_name: transaction-service
      dockerfile_path: services/transaction-service/Dockerfile
      build_context: services
      run_build: true
      sonar_project_key: transaction-service   # ✅ unique Sonar key

    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}