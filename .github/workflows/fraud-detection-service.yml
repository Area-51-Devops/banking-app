name: Fraud-Detection-Service-CI

on:
  push:
    paths:
      - 'services/fraud-detection-service/**'
  pull_request:
    paths:
      - 'services/fraud-detection-service/**'
  workflow_dispatch:

jobs:
  call-common:
    uses: ./.github/workflows/common-ci.yml
    with:
      working_directory: services/fraud-detection-service
      image_name: fraud-detection-service
      run_build: true
      sonar_project_key: fraud-detection-service   # ✅ unique Sonar key

    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}