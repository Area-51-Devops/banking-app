name: Reporting-Service-CI

on:
  push:
    paths:
      - 'services/reporting-service/**'
  pull_request:
    paths:
      - 'services/reporting-service/**'
  workflow_dispatch:

jobs:
  call-common:
    uses: ./.github/workflows/common-ci.yml
    with:
      working_directory: services/reporting-service
      image_name: reporting-service
      dockerfile_path: services/reporting-service/
      build_context: services
      run_build: true
      sonar_project_key: reporting-service   # ✅ unique Sonar key

    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}